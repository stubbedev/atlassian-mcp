# dependencies
node_modules/

# build output
dist/

# config & secrets
.env
.atlassian-mcp.json

# locks (keep package-lock.json if you want reproducible installs, remove this line if so)
# package-lock.json
